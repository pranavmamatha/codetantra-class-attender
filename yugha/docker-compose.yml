# Docker Compose for Auto Class Joiner with Dashboard
services:
  class-joiner:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USER_ID: "${USER_ID:-1001}"
        GROUP_ID: "${GROUP_ID:-1001}"
    container_name: auto-class-joiner-yugha
    restart: unless-stopped
    volumes:
      - ./config.json:/app/config.json:ro
      - ./logs_yugha:/app/logs_yugha
      - ./screenshots_yugha:/app/screenshots_yugha
      - chrome_data_yugha:/home/appuser/.config/google-chrome
    environment:
      - TZ=Asia/Kolkata
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '0.5'
    security_opt:
      - no-new-privileges:true
    healthcheck:
      test: ["CMD", "python", "-c", "import os; exit(0 if os.path.exists('/app/logs_yugha/class_joiner.log') else 1)"]
      interval: 60s
      timeout: 30s
      retries: 3
      start_period: 30s
    networks:
      - class-joiner-network-yugha

  dashboard:
    build:
      context: .
      dockerfile: Dockerfile.dashboard
    container_name: class-joiner-dashboard-yugha
    restart: unless-stopped
    ports:
      - "5003:5003"
    volumes:
      - ./logs_yugha:/app/logs_yugha
      - ./screenshots_yugha:/app/screenshots_yugha
    networks:
      - class-joiner-network-yugha
    environment:
      - TZ=Asia/Kolkata
      - FLASK_ENV=production
    depends_on:
      - class-joiner
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5003/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  chrome_data_yugha:
    driver: local

networks:
  class-joiner-network-yugha:
    driver: bridge